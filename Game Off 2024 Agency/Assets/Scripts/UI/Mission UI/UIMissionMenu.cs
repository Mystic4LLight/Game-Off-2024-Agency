using UnityEngine;

public class UIMissionMenu : MonoBehaviour
{
    [SerializeField] private Mission selectedMission;

    public Mission SelectedMission { get => selectedMission; set => selectedMission = value; }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
